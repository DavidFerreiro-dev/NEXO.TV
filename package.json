{
  "name": "nexo-tv",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp movies.json dist/ && html-minifier --collapse-whitespace --remove-comments --input-dir . --output-dir dist --file-ext html"
  },
  "devDependencies": {
    "html-minifier": "^4.0.0"
  }
}